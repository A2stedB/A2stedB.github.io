---
// import * as content from "../project-page/test-project.md";
// const posts = Object.values(
// 	import.meta.glob("./project-page/*.md", { eager: true }),
// );

// import { Content as RenderMD } from "../project-page/test-project.md";

// import { getEntry, render } from "astro:content";

// const { Content } = await render(RenderMD);
---

<h2>C++ 物理引擎与图形渲染</h2>
<p>在这个项目中，我将 GLFW 与 Box2D 物理库进行了深度整合。</p>

<h3>核心技术难点</h3>
<ul>
	<li>处理连续更新逻辑 (Continuous Update Logic)</li>
	<li>实现自定义的刚体变换 (Body Transformations)</li>
	<li>精准的事件处理与内存管理</li>
</ul>
